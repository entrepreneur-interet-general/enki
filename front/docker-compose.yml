version: '3.5'

services:
  front:
    build: .
    ports:
      - "4200:4200"
    volumes:
      - ./src:/app/src
    networks:
      - default
      - enki_default

networks:
  default:
    driver: bridge
  enki_default:
    external: true
