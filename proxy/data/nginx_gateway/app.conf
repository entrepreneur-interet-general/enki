server {

    listen 443;

    location / {
       proxy_pass http://kong:8000/enki/;
       proxy_set_header Host $host;
   }
   location /api/ {
       proxy_pass http://kong:8000/api/;
       proxy_set_header Host $host;
   }
}