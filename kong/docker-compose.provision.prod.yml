version: '2'

networks:
  default:
    driver: bridge
  enki_default:
    external: true

services:
  kong-provision:
    build:
      context: ./docker
      dockerfile: Dockerfile.provision
    env_file:
      - example.env.prod
    networks:
      - default
      - enki_default

