import { FilterLabelsPipe } from './filter-labels.pipe';

describe('FilterLabelsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLabelsPipe();
    expect(pipe).toBeTruthy();
  });
});
